import 'package:flutter/material.dart';

class Page5 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Hedef 2 Sayfa")),
      body: Center(child: Text("Buradasın!")),
    );
  }
}
